export type CardsFeedProps = {
    icon: string;
    description: string;
    name: string;
    author: string;
}

export type AccordionProps = {
    title: string;
}

export type MembersCardsProps = {
    img: string;
    name:string;
    rm:number;
    link: string;
}